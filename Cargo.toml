[package]
name = "goodmorning-bindings"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1", features = ["derive"]}

[features]
default = ["debug", "services-v1", "res-serde", "restrait", "req-serde"]
debug = []

res-serde = ["res-ser", "res-de"]
res-ser = ["res-serde-any"]
res-de = ["res-serde-any"]
res-serde-any = []

req-serde = ["req-ser", "req-de"]
req-ser = ["req-serde-any"]
req-de = ["req-serde-any"]
req-serde-any = []

services-v1 = []

restrait = []
